import { View, Text } from 'react-native';

export default function SavedScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-semibold text-gray-800">Saved Homes</Text>
    </View>
  );
}
